<%= form_for(nyanchu) do |f| %>
  <% if nyanchu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nyanchu.errors.count, "error") %> prohibited this nyanchu from being saved:</h2>

      <ul>
      <% nyanchu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
