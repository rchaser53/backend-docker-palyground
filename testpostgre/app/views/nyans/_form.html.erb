<%= form_for(nyan) do |f| %>
  <% if nyan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nyan.errors.count, "error") %> prohibited this nyan from being saved:</h2>

      <ul>
      <% nyan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
